# Dependencies
node_modules/
*/node_modules/

# Production builds
dist/
*/dist/
build/
*/build/

# Environment variables
.env*
!.env.example
*.env
*.env.local

# Logs
*.log

# OS generated
.DS_Store
Thumbs.db

# Local tooling config — machine-specific, never pushed
.claude/
HackHeritage/
extras/

# Python
.venv/
__pycache__/
*.pyc

# Vite dev cache (only appears if the dev server is started from the wrong root)
.vite/

# Scratch space for unpacking the WDPA shapefile download — the built
# GeoJSON is committed, the raw archive is not.
backend/data/geo/_wdpa_work/
